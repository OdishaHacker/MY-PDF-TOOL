version: '3.8'

services:
  pdf-toolkit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pdf-toolkit
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
